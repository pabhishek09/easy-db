Usage

const easyDb = require('easy-db');

easyDb.connect();

Initilise an express app with two endpoints

1. /api

/collections : get
/table 

Collection data http://localhost:5000/data?collection='users'

Table data http://localhost:5000/data?collection='users'&table='test'
Support: 
[Insert row, delete row, get row, update row ]

2. Expose browser endpoint to interact with data
http://localhost:5000/easyDb

Refactor to support multiple tables

Create two different servers - 
One for data API's
One for application and supported API'servers
